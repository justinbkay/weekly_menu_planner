<h2>Recipes</h2>
<p>
  <%= link_to "New", new_user_recipe_path(current_user) %>
</p>
<ul>
  <% @recipes.each do  |r| %>
    <li><%= r.name %> - <%= link_to 'edit', edit_user_recipe_path(current_user, r) %></li>
  <% end %>
</ul>